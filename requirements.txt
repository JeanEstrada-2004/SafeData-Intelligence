fastapi==0.104.1          # Framework web rÃ¡pido y moderno para crear APIs en Python (basado en ASGI).
uvicorn==0.24.0           # Servidor ASGI ligero y rÃ¡pido que ejecuta aplicaciones FastAPI.
sqlalchemy==2.0.23        # ORM para manejar bases de datos relacionales como PostgreSQL desde Python.
psycopg2-binary==2.9.9    # Driver que permite conectar Python con PostgreSQL (usado por SQLAlchemy).
python-multipart==0.0.6   # Permite manejar formularios y archivos enviados en peticiones HTTP (multipart/form-data).
jinja2==3.1.2             # Motor de plantillas HTML, Ãºtil si tu FastAPI genera pÃ¡ginas web dinÃ¡micas.
pandas==2.1.3             # LibrerÃ­a para anÃ¡lisis y manipulaciÃ³n de datos (muy usada para procesar datasets).
openpyxl==3.1.2           # Permite leer y escribir archivos Excel (.xlsx), ideal si importas/exportas datos.
python-dotenv==1.0.0      # Carga variables de entorno desde un archivo .env (para configurar credenciales o rutas).
requests==2.31.0        # Cliente HTTP para consumir el servicio de geocodificación Nominatim.

email-validator==2.1.0.post1  # Validación de email para formularios
passlib[bcrypt]==1.7.4        # Hash/verify de contraseñas (bcrypt)
PyJWT==2.8.0                  # JWT para cookie de sesión
geopy==2.4.1    # Geocoding (Nominatim)
